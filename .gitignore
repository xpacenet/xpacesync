node_modules/
coverage/
*.log

# Generated by `tsc` (npm run build:types) from src/**/*.js's own JSDoc —
# single source of truth is the JSDoc, not a checked-in artifact. Runs
# automatically via the "prepare" script for anyone installing this as a
# git dependency (npm's documented behavior for git-sourced packages).
types/
